import { Product } from '@/app/models/interfaces'
import React from 'react'

export default function productCard
({title, price, description, image} : Product) {
  return (
    <div>productCard</div>
  )
}
